In a sharded cluster, which of the following can keep large chunks from being split as part of MongoDB's balancing process? Check all that apply

Frequent restarts of all mongos processes relative to the number of writes[T]
If there are not enough distinct shard key values[T]
If one of the config servers is down when a mongos tries to do a split[T]
The average size of the documents in the chunk
The number of shards in the cluster
